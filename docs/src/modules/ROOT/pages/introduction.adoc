[#introduction]
:page-aliases: ../index.adoc, planning-ai-concepts.adoc
:doctype: book
:sectnums:
:icons: font

[#whatIsGreyCOS]
= Introduction

Every organization faces planning problems: providing products or services with a limited set of _constrained_ resources (employees, assets, time, and money).
https://github.com/CameleoGrey/greycos-solver[GreyCOS Solver] is a lightweight,
embeddable constraint satisfaction engine that optimizes these problems using constraint satisfaction programming.

Typical use cases include vehicle routing, employee scheduling, rostering, bin packing, and equipment scheduling.

== Planning problems

A planning problem arises from a need to achieve a *goal* by organizing available *resources* under a set of *constraints*:

* Building a house, where workers and materials are the resources, and construction order and site hours are the constraints.
* Staffing a hospital shift, where qualified personnel are the resources, and working-hour rules and required specializations are the constraints.
* Routing delivery vehicles, where vehicles and drivers are the resources, and time windows, capacity, and distance are the constraints.

Planning problems grow combinatorially as resources and constraints are added.
Even a small field service problem with 4 vehicles and 8 visits produces over 19 million possible assignments for a brute-force algorithm to consider.
GreyCOS Solver evaluates this space efficiently and finds good solutions in a fraction of the time it would take to plan manually.

== Constraints

Constraints can be classified as *hard*, *medium*, or *soft*:

* *Hard constraints* are rules that must not be broken, such as an employee only being in one place at a time.
  Violating a hard constraint produces an infeasible plan.
* *Medium constraints* apply when resources are scarce, such as assigning as many customer visits as possible when there are not enough technicians.
* *Soft constraints* express business preferences, such as minimizing travel time or assigning employees to preferred shifts.

Solutions are scored to reflect how well they satisfy these constraints.
For example: `0hard/-257medium/-6119520soft`.
A solution with zero hard constraint violations is always better than one that breaks even a single hard constraint,
regardless of the soft score.

== Technology

GreyCOS Solver is 100% pure Java^TM^ and runs on Java {java-version} or higher, on any JVM platform.
It xref:integration/integration.adoc#integration[integrates with Quarkus and Spring Boot] and supports Kotlin.

:!sectnums:
== Next

* Follow the xref:quickstart/overview.adoc[quickstart] to tackle your first planning problem.
