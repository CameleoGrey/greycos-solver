<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">

  <modelVersion>4.0.0</modelVersion>
  <parent>
    <groupId>ai.greycos.solver</groupId>
    <artifactId>greycos-solver-build-parent</artifactId>
    <version>999-SNAPSHOT</version>
    <relativePath>../build/build-parent/pom.xml</relativePath>
  </parent>

  <artifactId>greycos-solver-spring-integration</artifactId>
  <packaging>pom</packaging>

  <name>GreyCOS Solver Spring integration</name>
  <description>
    GreyCOS solves planning problems.
    This lightweight, embeddable planning engine implements powerful and scalable algorithms
    to optimize business resource scheduling and planning.

    This module contains the integration modules.
  </description>
  <url>https://github.com/CameleoGrey/greycos-solver</url>

  <modules>
    <module>spring-boot-autoconfigure</module>
    <module>spring-boot-starter</module>
    <module>spring-boot-integration-test</module>
  </modules>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <!--
          GreyCOS Solver's Jackson 3 version is independent of Spring Boot.
          Import this first so that the persistence and Spring modules stay aligned.
        -->
        <groupId>tools.jackson</groupId>
        <artifactId>jackson-bom</artifactId>
        <version>${version.tools.jackson}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
      <dependency>
        <!-- Import dependency management from Spring Boot. -->
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-dependencies</artifactId>
        <version>${version.org.springframework.boot}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
    </dependencies>
  </dependencyManagement>

</project>
