<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">

  <modelVersion>4.0.0</modelVersion>
  <parent>
    <groupId>io.github.cameleogrey</groupId>
    <artifactId>greycos-solver-spring-integration</artifactId>
    <version>999-SNAPSHOT</version>
  </parent>

  <artifactId>greycos-solver-spring-boot-starter</artifactId>

  <name>GreyCOS Solver Spring Boot - Starter</name>
  <description>
    GreyCOS solves planning problems.
    This lightweight, embeddable planning engine implements powerful and scalable algorithms
    to optimize business resource scheduling and planning.

    This module contains the Spring Boot starter.
  </description>
  <url>https://github.com/CameleoGrey/greycos-solver</url>
  <dependencies>
    <!-- Internal dependencies -->
    <dependency>
      <groupId>org.springframework</groupId>
      <artifactId>spring-core</artifactId>
      <exclusions>
        <exclusion>
          <groupId>commons-logging</groupId>
          <artifactId>commons-logging</artifactId>
        </exclusion>
      </exclusions>
    </dependency>
    <dependency>
      <groupId>io.github.cameleogrey</groupId>
      <artifactId>greycos-solver-spring-boot-autoconfigure</artifactId>
    </dependency>
    <dependency>
      <groupId>io.github.cameleogrey</groupId>
      <artifactId>greycos-solver-core</artifactId>
    </dependency>
    <dependency>
      <groupId>io.github.cameleogrey</groupId>
      <artifactId>greycos-solver-jackson</artifactId>
    </dependency>
    <!-- Required by Spring Boot reference guide. -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-persistence</artifactId>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <artifactId>maven-dependency-plugin</artifactId>
        <executions>
          <execution>
            <!-- Starter is essentially a "dependency chain". It has unused dependencies by definition. -->
            <id>analyze-only</id>
            <phase>none</phase>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
</project>
