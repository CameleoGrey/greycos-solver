/**
 * Configuration for the island model phase.
 *
 * <p>The island model runs multiple independent island agents in parallel, each running the same
 * phases independently. Agents periodically exchange their best solutions through migration in a
 * ring topology.
 */
package ai.greycos.solver.core.config.islandmodel;
